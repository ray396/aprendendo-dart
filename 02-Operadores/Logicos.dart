void main() {
  print('02.1 - Operadores logicos');

  var verdadeiro = !false;
  bool falso = !true;

  print('Verdadeiro = ${verdadeiro} falso = ${falso}');

  //operador && (e) somente se ambas afirmações for true o retornor sera true
  //operador || (ou) precisa somente de uma afirmação verdadeira para retornar true
  //operador ! (not) inverte o valor boleano
}
